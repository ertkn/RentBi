/*
import 'package:capstone/utilities/spacing.dart';
import 'package:flutter/material.dart';

class BuildDivider extends StatelessWidget {
  List<double> percentageLeftWidth;
  double percentageHeight;

  BuildDivider({
    Key? key,
    required this.percentageLeftWidth,
    required this.percentageHeight,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: screenHeightPercentage(context, percentage: this.percentageHeight),
      color: const Color(0xFF0E2B4E),
      thickness: 1,
      indent: screenWidthPercentage(context, percentage: percentageLeftWidth[0]),
      endIndent: screenWidthPercentage(context, percentage: percentageLeftWidth[1]),
    );
  }
}
*/
